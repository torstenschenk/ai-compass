Use the following code to create the results page:

import { Navigation } from './Navigation';
import { ResultsHero } from './ResultsHero';
import { ClusterProfile } from './ClusterProfile';
import { MaturityProfile } from './MaturityProfile';
import { GapAnalysis } from './GapAnalysis';
import { Roadmap } from './Roadmap';
import { ExpertConsultation } from './ExpertConsultation';
import { DownloadCTA } from './DownloadCTA';
import { Footer } from './Footer';

export function ResultsPage() {
  return (
    <div className="min-h-screen bg-white">
      <Navigation />
      <ResultsHero />
      <ClusterProfile />
      <MaturityProfile />
      <GapAnalysis />
      <Roadmap />
      <ExpertConsultation />
      <DownloadCTA />
      <Footer />
    </div>
  );
}

```md
# results_page_spec.md

## Results Page (Analysis and Recommendations) — MVP Spec

### 1) Goal
Receive instant analysis on AI readiness and provide an immediate roadmap and action plan.

### 2) Data Source (Single Source of Truth)
- `GET /api/v1/assessments/{assessment_id}?token={access_token}`

**Response must include (minimum)**
- Company: `company_name`, `industry`, `employee_band`, optional `website`, `city`
- Assessment meta: `resopnse_id`, `status`, `created_at`, `completed_at`, `schema_hash`
- Scores:
  - `overall_score_1_5` (display value: **2.6/5**)
  - `company_cluster_name` (e.g., “The Structured Builder”)
  - `percentile` + `benchmark_context` (e.g., “Top 32% in Manufacturing”)
- Dimension scores (7): name + score (1–5)
- Drivers / key insights (if available): top items per weakest dimensions
- Roadmap + recommendations payload (LLM or fallback)
- PDF availability: `pdf_url` or a flag to enable the download button

### 3) Page Layout (UI Components)
Order and presence are fixed for MVP:

1) **Navigation** (2-Navigation.md)
2) **Header (Executive Results Dashboard Header)** (8.1-Header.md)
   - Title + subtitle
   - 3 data cards:
     - Total Overall Score (**2.6/5**)
     - Company Cluster (dynamic name)
     - Percentile (dynamic, includes industry context)
3) **Cluster Profile Visualization** (8.2-Cluster-Profile.md)
   - 5-bar chart (low → high)
   - Active cluster highlighted
   - Description cards (active one highlighted)
4) **Multi-Dimensional Radar Chart** (8.3-Multi-Dimensional-Radar-Chart.md)
   - 7 dimensions (Strategy, People, Data, Use Cases, Processes, Compliance, Tech)
   - Compare Your Company vs Peer Benchmark (all companys in db on avg)
5) **Strategic Gap Analysis**  (8.4-Strategic-Gap-Analysis.md)
6) **Roadmap** (8.5-Roadmap.md)
  7) **Expert Consultation & Next Steps** (8.6-Expert-Consultation.md)
8) **Download Report (PDF only)** (8.7-Download-Report.md)
9) **Footer** (2-Footer.md)

### 4) Behavior (MVP)
- Page loads by `assessment_id` + token.
- If `status != completed`:
  - Show a clear “Assessment not completed yet” state and block insights that require completion.
- All charts render only when required data is present.
- Download button calls:
  - `GET /api/v1/assessments/{assessment_id}/pdf?token={access_token}`

### 5) Outcome
User sees “where we are” and “what to do next” without ambiguity.
